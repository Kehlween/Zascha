import React from "react";
import { ListItem, ListItemAvatar, Container } from '@material-ui/core';
import { GradientBackground, SectionTitle } from "./_about.styled";

const About = () => {
    return (
        <GradientBackground>
            <Container>
            <SectionTitle>Quem vai te ajudar:</SectionTitle>

            </Container>
        </GradientBackground>
    );
};

export default About;